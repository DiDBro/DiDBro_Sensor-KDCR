# 📚 课件中所有带括号编号的公式汇总（LaTeX）

> 来源：`Nico_Robo_control/课件/` 下全部 13 个 PDF
> 公式来自 MLS (Murray, Li & Sastry) 教材

---

## March_2NT.pdf — SO(3), 旋转矩阵, 指数坐标

| 编号 | 公式 (LaTeX) |
|------|-------------|
| `(2.1)` | $RR^T = R^T R = I$ |
| `(2.2)` | $SO(n) = \bigl\{R \in \mathbb{R}^{n \times n} \;\big|\; RR^T = I,\ \det R = +1\bigr\}$ |
| `(2.3)` | $R_{ac} = R_{ab}\,R_{bc}$ |
| `(2.4)` | $\widehat{a} = \begin{bmatrix} 0 & -a_3 & a_2 \\ a_3 & 0 & -a_1 \\ -a_2 & a_1 & 0 \end{bmatrix}$ （hat / 反对称算子） |
| `(2.5)` | $a \times b = \widehat{a}\,b$ |
| `(2.6)` | $R\,(v \times w) = (Rv) \times (Rw)$ |
| `(2.7)` | $R\,\widehat{w}\,R^T = \widehat{Rw}$ |
| `(2.8)` | $\dot{q}(t) = \omega \times q(t) = \widehat{\omega}\,q(t)$ |
| `(2.9)` | $R(\omega,\theta) = e^{\widehat{\omega}\theta}$ |
| `(2.11)` | $e^{\widehat{\omega}\theta} = I + \widehat{\omega}\theta + \frac{\theta^2}{2!}\widehat{\omega}^2 + \frac{\theta^3}{3!}\widehat{\omega}^3 + \cdots$ |
| `(2.17)` | $\displaystyle \theta = \cos^{-1}\!\left(\frac{\operatorname{tr}(R) - 1}{2}\right)$ |
| `(2.18)` | $\displaystyle \omega = \frac{1}{2\sin\theta} \begin{bmatrix} r_{32} - r_{23} \\ r_{13} - r_{31} \\ r_{21} - r_{12} \end{bmatrix}, \quad \theta \neq 0$ |

---

## March_9NT.pdf — 刚体运动, SE(3), 指数映射

| 编号       | 公式 (LaTeX)                                                                                                                                                                                                      |                                                            |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| `(2.14)` | $e^{\widehat{\omega}\theta} = I + \widehat{\omega}\sin\theta + \widehat{\omega}^2(1 - \cos\theta)$ （Rodrigues 公式）                                                                                               |                                                            |
| `(2.22)` | $q_a = p_{ab} + R_{ab}\,q_b$                                                                                                                                                                                    |                                                            |
| `(2.24)` | $\bar{g}_{ac} = \bar{g}_{ab}\,\bar{g}_{bc} = \begin{bmatrix} R_{ab}R_{bc} & R_{ab}p_{bc} + p_{ab} \\ 0 & 1 \end{bmatrix}$                                                                                       |                                                            |
| `(2.25)` | $\dot{p}(t) = \omega \times \bigl(p(t) - q\bigr)$                                                                                                                                                               |                                                            |
| `(2.26)` | $\widehat{\xi} = \begin{bmatrix} \widehat{\omega} & v \\ 0 & 0 \end{bmatrix}, \quad v = -\omega \times q$                                                                                                       |                                                            |
| `(2.27)` | $\dot{p}(t) = v$ （纯平移）                                                                                                                                                                                          |                                                            |
| `(2.28)` | $\begin{bmatrix} \dot{p} \\ 0 \end{bmatrix} = \widehat{\xi}\begin{bmatrix} p \\ 1 \end{bmatrix}, \quad \widehat{\xi} = \begin{bmatrix} 0_{3\times3} & v \\ 0_{1\times3} & 0 \end{bmatrix}$                      |                                                            |
| `(2.29)` | $se(3) := \bigl\{(v,\,\widehat{\omega}) \;\big                                                                                                                                                                  | \; v \in \mathbb{R}^3,\ \widehat{\omega} \in so(3)\bigr\}$ |
| `(2.30)` | $\begin{bmatrix} \widehat{\omega} & v \\ 0 & 0 \end{bmatrix}^{\vee} = \begin{bmatrix} v \\ \omega \end{bmatrix}$ （vee 算子）                                                                                       |                                                            |
| `(2.31)` | $\begin{bmatrix} v \\ \omega \end{bmatrix}^{\wedge} = \begin{bmatrix} \widehat{\omega} & v \\ 0 & 0 \end{bmatrix}$ （wedge 算子）                                                                                   |                                                            |
| `(2.32)` | $e^{\widehat{\xi}\theta} = \begin{bmatrix} I & v\theta \\ 0 & 1 \end{bmatrix}, \quad \omega = 0$                                                                                                                |                                                            |
| `(2.33)` | $g = \begin{bmatrix} I & \widehat{\omega}v \\ 0 & 1 \end{bmatrix}$                                                                                                                                              |                                                            |
| `(2.34)` | $\widehat{\xi}' = g^{-1}\,\widehat{\xi}\,g = \begin{bmatrix} \widehat{\omega} & \omega\omega^T v \\ 0 & 0 \end{bmatrix}$                                                                                        |                                                            |
| `(2.35)` | $e^{\widehat{\xi}\theta} = g\,e^{\widehat{\xi}'\theta}\,g^{-1}$                                                                                                                                                 |                                                            |
| `(2.36)` | $\displaystyle e^{\widehat{\xi}\theta} = \begin{bmatrix} e^{\widehat{\omega}\theta} & (I - e^{\widehat{\omega}\theta})\widehat{\omega}v + \omega\omega^T v\,\theta \\ 0 & 1 \end{bmatrix}, \quad \omega \neq 0$ |                                                            |
| `(2.37)` | $g_{ab}(\theta) = e^{\widehat{\xi}\theta}\,g_{ab}(0)$                                                                                                                                                           |                                                            |
| `(2.38)` | $(I - e^{\widehat{\omega}\theta})\,\widehat{\omega}v + \omega\omega^T v\,\theta = p$                                                                                                                            |                                                            |

---

## March_16NT.pdf — 螺旋运动, 线几何

| 编号       | 公式 (LaTeX)                                                                                                                                                                                 |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `(2.36)` | $\displaystyle e^{\widehat{\xi}\theta} = \begin{bmatrix} e^{\widehat{\omega}\theta} & (I - e^{\widehat{\omega}\theta})\widehat{\omega}v + \omega\omega^T v\,\theta \\ 0 & 1 \end{bmatrix}$ |
| `(2.39)` | $l = \{q + \lambda\omega \mid \lambda \in \mathbb{R}\}$ （螺旋轴）                                                                                                                              |
| `(2.40)` | $\displaystyle g = \begin{bmatrix} e^{\widehat{\omega}\theta} & (I - e^{\widehat{\omega}\theta})q + h\omega\theta \\ 0 & 1 \end{bmatrix}$ （螺旋运动）                                           |
| `(2.41)` | $h = \dfrac{\omega^T v}{\|\omega\|^2}$ （螺距）; $h = \infty$ 时为纯平移                                                                                                                            |
| `(2.42)` | $\|\omega\|^2$ （用于螺距和轴上点的分母）                                                                                                                                                               |
| `(2.43)` | 螺旋轴：若 $\omega \neq 0$: $l = \{q + \lambda\omega \mid \lambda \in \mathbb{R}\}$; 若 $\omega = 0$: $\{0 + \lambda v \mid \lambda \in \mathbb{R}\}$                                            |
| `(2.44)` | 幅值 $M$：若 $\omega \neq 0$: $M = \|\omega\|$; 若 $\omega = 0$: $M = \|v\|$                                                                                                                    |

---

## March_23NT.pdf — 刚体速度, Adjoint 变换

| 编号 | 公式 (LaTeX) |
|------|-------------|
| `(2.45)` | $g_{ab}(\theta) = e^{\widehat{\xi}\theta}\,g_{ab}(0)$ |
| `(2.46)` | $v_{q_a}(t) = \frac{d}{dt}q_a(t) = \dot{R}_{ab}(t)\,q_b$ |
| `(2.47)` | $\dot{R}_{ab}(t)\,q_b = \dot{R}_{ab}(t)\,R_{ab}^{-1}(t)\,q_a(t)$ |
| `(2.48)` | $\widehat{\omega}_{ab}^s = \dot{R}_{ab}\,R_{ab}^{-1}$ （空间角速度） |
| `(2.49)` | $\widehat{\omega}_{ab}^b = R_{ab}^{-1}\,\dot{R}_{ab}$ （物体角速度） |
| `(2.50)` | $\widehat{\omega}_{ab}^b = R_{ab}^{-1}\,\widehat{\omega}_{ab}^s\,R_{ab}$ 或 $\omega_{ab}^b = R_{ab}^{-1}\,\omega_{ab}^s$ |
| `(2.52)` | $v_{q_b}(t) = \widehat{\omega}_{ab}^b(t)\,q_b$ |
| `(2.53)` | $V_{ab}^s = \begin{bmatrix} v_{ab}^s \\ \omega_{ab}^s \end{bmatrix} = \begin{bmatrix} -\dot{R}_{ab}R_{ab}^T p_{ab} + \dot{p}_{ab} \\ (\dot{R}_{ab}R_{ab}^T)^{\vee} \end{bmatrix}$ （空间速度 twist） |
| `(2.55)` | $V_{ab}^b = \begin{bmatrix} v_{ab}^b \\ \omega_{ab}^b \end{bmatrix} = \begin{bmatrix} R_{ab}^T\,\dot{p}_{ab} \\ (R_{ab}^T\,\dot{R}_{ab})^{\vee} \end{bmatrix}$ （物体速度 twist） |
| `(2.58)` | $\operatorname{Ad}_g = \begin{bmatrix} R & \widehat{p}R \\ 0 & R \end{bmatrix} \in \mathbb{R}^{6\times6}$ （Adjoint 变换矩阵） |
| `(2.61)` | $V^s = \operatorname{Ad}_g\,V^b$ （空间与物体速度关系） |

---

## March_30NT.pdf — Wrenches（力旋量）

| 编号 | 公式 (LaTeX) |
|------|-------------|
| `(2.53)` | $V_{ab}^s = \begin{bmatrix} v_{ab}^s \\ \omega_{ab}^s \end{bmatrix}$ （空间速度） |
| `(2.55)` | $V_{ab}^b = \begin{bmatrix} v_{ab}^b \\ \omega_{ab}^b \end{bmatrix}$ （物体速度） |
| `(2.61)` | $V^s = \operatorname{Ad}_g\,V^b$ |
| `(2.65)` | $F_c = \operatorname{Ad}_{g_{bc}}^T\,F_b$ |
| `(2.66)` | $\begin{bmatrix} f_c \\ \tau_c \end{bmatrix} = \begin{bmatrix} R_{bc}^T & 0 \\ -R_{bc}^T\,\widehat{p}_{bc} & R_{bc}^T \end{bmatrix} \begin{bmatrix} f_b \\ \tau_b \end{bmatrix}$ |
| `(2.67)` | $F_b = \operatorname{Ad}_g^T\,F_s$ （力旋量的 Adjoint 变换） |
| `(2.68)` | $F = \begin{cases} M\begin{bmatrix} \omega \\ -\widehat{\omega}q + h\omega \end{bmatrix}, & h\text{ 有限} \\[8pt] M\begin{bmatrix} 0 \\ \omega \end{bmatrix}, & h = \infty \end{cases}$ |
| `(2.69)` | $\|f\|^2$ （力旋量中力分量的范数） |
| `(2.70)` | 力旋量轴线：若 $f \neq 0$: $\{q + \lambda f + \lambda\tau \mid \lambda \in \mathbb{R}\}$; 若 $f = 0$: $\{0 + \lambda\tau \mid \lambda \in \mathbb{R}\}$ |
| `(2.71)` | 力旋量幅值：若 $f \neq 0$: $\|f\|$; 若 $f = 0$: $\|\tau\|$ |

---

## April_20_manip_kinemNT.pdf — 正运动学 POE

| 编号 | 公式 (LaTeX) |
|------|-------------|
| `(2.35)` | $e^{\widehat{\xi}\theta} = g\,e^{\widehat{\xi}'\theta}\,g^{-1}$ |
| `(3.1)` | $g_{st}(\theta) = g_{sl_1}(\theta_1)\,g_{l_1 l_2}(\theta_2)\,\cdots\,g_{l_{n-1}l_n}(\theta_n)\,g_{l_n t}$ |
| `(3.2)` | $g_{st}(\theta_1,\theta_2) = e^{\widehat{\xi}_1\theta_1}\,g_{st}(\theta_2) = e^{\widehat{\xi}_1\theta_1}\,e^{\widehat{\xi}_2\theta_2}\,g_{st}(0)$ |
| `(3-7)` | $g_{l_{i-1}l_i}(\theta_i) = e^{\widehat{\xi}_{i-1,i}\,\theta_i}\,g_{l_{i-1}l_i}(0)$ |
| `(3-10)` | $\xi_i = \operatorname{Ad}_{g_{l_0 l_{i-1}}(0)}\,\xi_{i-1,i}$ |

---

## April_27_inv_kinNT.pdf — 逆运动学

| 编号 | 公式 (LaTeX) |
|------|-------------|
| `(3.18)` | $e^{\widehat{\xi}\theta}(u + r) = v + r \;\Longrightarrow\; e^{\widehat{\omega}\theta}\,u = v$ （Paden-Kahan 子问题 1） |
| `(3.25)` | $\displaystyle \alpha = \frac{(\omega_1 \times \omega_2)^T(u \times v) - (\omega_1 \times \omega_2)^T(\omega_2 \times u)}{\|\omega_1 \times \omega_2\|^2}$ （Paden-Kahan 子问题 2） |

---

## May_11_end_effector_vel_forcNT.pdf — 末端速度与力, Jacobian

| 编号 | 公式 (LaTeX) |
|------|-------------|
| `(2.53)` | $V_{ab}^s = \begin{bmatrix} v_{ab}^s \\ \omega_{ab}^s \end{bmatrix}$ |
| `(2.54)` | $\dot{q}_s = v_{ab}^s + \omega_{ab}^s \times q_s$ （空间坐标系中点的速度） |
| `(2.56)` | $\dot{q}_b = v_{ab}^b + \omega_{ab}^b \times q_b$ （物体坐标系中点的速度） |
| `(3.51)` | $V_{st}^s = \sum_{i=1}^{n}\left(\frac{\partial g_{st}}{\partial \theta_i}\,g_{st}^{-1}\right)^{\vee}\,\dot{\theta}_i$ |
| `(3.52)` | $V_{st}^s = J_{st}^s(\theta)\,\dot{\theta}, \quad J_{st}^s(\theta) = \begin{bmatrix} \left(\frac{\partial g_{st}}{\partial\theta_1}g_{st}^{-1}\right)^{\vee} & \cdots & \left(\frac{\partial g_{st}}{\partial\theta_n}g_{st}^{-1}\right)^{\vee} \end{bmatrix}$ |
| `(3.54)` | $\left(\frac{\partial g_{st}}{\partial\theta_i}g_{st}^{-1}\right)^{\vee} = \operatorname{Ad}_{g_{s,i-1}}\,\xi_i, \quad g_{s,i-1} = e^{\widehat{\xi}_1\theta_1}\cdots e^{\widehat{\xi}_{i-1}\theta_{i-1}}$ |
| `(3.55)` | $\left(\frac{\partial g_{st}}{\partial\theta_i}g_{st}^{-1}\right)^{\vee} = \operatorname{Ad}_{g_{i,t}}\,\xi_i, \quad g_{i,t} = e^{\widehat{\xi}_i\theta_i}\cdots e^{\widehat{\xi}_n\theta_n}\,g_{st}(0)$ |
| `(3.59)` | $\tau = (J_{st}^s)^T\,F_t$ （关节力矩与末端力关系，空间坐标系） |
| `(3.60)` | $\tau = (J_{st}^b)^T\,F_s$ （关节力矩与末端力关系，物体坐标系） |

---

## May_18_H_T_Newton_EulerNT.pdf — 动能, Newton-Euler 方程

| 编号 | 公式 (LaTeX) |
|------|-------------|
| `(2.55)` | $V_{ab}^b = \begin{bmatrix} v_{ab}^b \\ \omega_{ab}^b \end{bmatrix} = \begin{bmatrix} R_{ab}^T\,\dot{p}_{ab} \\ (R_{ab}^T\,\dot{R}_{ab})^{\vee} \end{bmatrix}$ |
| `(4.9)` | $T = \frac{1}{2}(V^b)^T \mathcal{M}\,V^b$ （动能，物体坐标系） |
| `(4.12)` | $f = m\,\ddot{p}$ （Newton 第二定律） |
| `(4.13)` | $\tau = \mathcal{I}'\,\dot{\omega}^s + \widehat{\omega}^s\,\mathcal{I}'\,\omega^s$ （Euler 方程，空间坐标系） |
| `(4.14)` | $f^b = m\,\dot{v}^b + \widehat{\omega}^b\,m\,v^b$ （Newton 方程，物体坐标系） |
| `(4.15)` | $\tau^b = \mathcal{I}\,\dot{\omega}^b + \widehat{\omega}^b\,\mathcal{I}\,\omega^b$ （Euler 方程，物体坐标系） |
| `(4.16)` | $F^b = \begin{bmatrix} mI & 0 \\ 0 & \mathcal{I} \end{bmatrix} \begin{bmatrix} \dot{v}^b \\ \dot{\omega}^b \end{bmatrix} + \begin{bmatrix} m\,\widehat{\omega}^b\,v^b \\ \widehat{\omega}^b\,\mathcal{I}\,\omega^b \end{bmatrix}$ （Newton-Euler，物体坐标系） |

---

## May_25_Dyn_OpenChainManNT.pdf — 开链动力学, EoM

| 编号 | 公式 (LaTeX) |
|------|-------------|
| `(3.55)` | $\left(\frac{\partial g_{st}}{\partial\theta_i}\,g_{st}^{-1}\right)^{\vee} = \operatorname{Ad}_{e^{\widehat{\xi}_i\theta_i}\,\cdots\,e^{\widehat{\xi}_n\theta_n}\,g_{st}(0)}\,\xi_i$ |
| `(4.9)` | $T_i = \frac{1}{2}(V_i^b)^T \mathcal{M}_i\,V_i^b$ |
| `(4.17)` | $T(\theta,\dot{\theta}) = \frac{1}{2}\dot{\theta}^T M(\theta)\,\dot{\theta}, \quad M(\theta) = \sum_{i=1}^{n} J_i(\theta)^T \mathcal{M}_i\,J_i(\theta)$ |
| `(4.18)` | $T = \sum_{i=1}^{n} T_i(\theta,\dot{\theta}) = \frac{1}{2}\dot{\theta}^T M(\theta)\,\dot{\theta}$ |
| `(4.20)` | $\displaystyle \frac{d}{dt}\frac{\partial L}{\partial\dot{\theta}_i} - \frac{\partial L}{\partial\theta_i} = \Upsilon_i, \quad L = T - V$ |
| `(4.21)` | $\displaystyle \sum_{j=1}^{n} M_{ij}(\theta)\,\ddot{\theta}_j + \sum_{j,k=1}^{n} \Gamma_{ijk}\,\dot{\theta}_j\,\dot{\theta}_k + \frac{\partial V}{\partial\theta_i} = \Upsilon_i$ |
| `(4.22)` | $\displaystyle \Gamma_{ijk} = \frac{1}{2}\left(\frac{\partial M_{ij}(\theta)}{\partial\theta_k} + \frac{\partial M_{ik}(\theta)}{\partial\theta_j} - \frac{\partial M_{kj}(\theta)}{\partial\theta_i}\right)$ （Christoffel 符号） |
| `(4.23)` | $\displaystyle C_{ij}(\theta,\dot{\theta}) = \sum_{k=1}^{n} \Gamma_{ijk}\,\dot{\theta}_k$ （Coriolis 矩阵） |
| `(4.24)` | $\boxed{M(\theta)\,\ddot{\theta} + C(\theta,\dot{\theta})\,\dot{\theta} + N(\theta,\dot{\theta}) = \tau}$ （运动方程） |
| `(4.27)` | $A_{ij} = \begin{cases} \operatorname{Ad}_{(e^{\widehat{\xi}_j\theta_j}\,\cdots\,e^{\widehat{\xi}_{i-1}\theta_{i-1}}\,g_{sl_i}(0))^{-1}}, & j < i \\ I, & j = i \\ 0, & j > i \end{cases}$ |
| `(4.28)` | $M(\theta) = \sum_{i=1}^{n} J_i^T(\theta)\,\mathcal{M}_i\,J_i(\theta), \quad J_i(\theta) = \begin{bmatrix} A_{i1}\xi_1 & \cdots & A_{ii}\xi_i & 0 & \cdots & 0 \end{bmatrix}$ |
| `(4.29)` | $\displaystyle \frac{\partial M_{ij}}{\partial\theta_k} = (A_{ik}\xi_k)^T M_i\,(A_{ij}\xi_j) + (A_{ij}\xi_j)^T M_i\,(A_{ik}\xi_k)$ |
| `(4.30)` | $\displaystyle \frac{\partial M_{kj}}{\partial\theta_i} = (A_{ki}[A_{ij}\xi_j, \xi_i])^T \mathcal{M}_k\,(A_{kj}\xi_j) + (A_{kj}\xi_j)^T \mathcal{M}_k\,(A_{ki}[A_{ij}\xi_j, \xi_i])$ |

---

## June_1_ControlNT.pdf — 控制

| 编号 | 公式 (LaTeX) |
|------|-------------|
| `(4.24)` | $M(\theta)\,\ddot{\theta} + C(\theta,\dot{\theta})\,\dot{\theta} + N(\theta,\dot{\theta}) = \tau$ |
| `(4.47)` | $M(\theta)\,\ddot{\theta} + C(\theta,\dot{\theta})\,\dot{\theta} + N(\theta,\dot{\theta}) = \tau$ （计算力矩控制） |
| `(4.49)` | $\tau = M(\theta)\bigl(\ddot{\theta}_d - K_v\,\dot{e} - K_p\,e\bigr) + C(\theta,\dot{\theta})\,\dot{\theta} + N(\theta,\dot{\theta})$ |

---

## June_1_gravitational_forcesNT.pdf — 重力

| 编号 | 公式 (LaTeX) |
|------|-------------|
| `(2.67)` | $F_b = \operatorname{Ad}_g^T\,F_s$ |
| `(4.24)` | $M(\theta)\,\ddot{\theta} + C(\theta,\dot{\theta})\,\dot{\theta} + N(\theta,\dot{\theta}) = \tau$ |

---

## MLS_ex_2_6_selfgen.pdf — 代码示例中的公式引用

| 编号 | 公式 (LaTeX) |
|------|-------------|
| `(2.14)` | $e^{\widehat{\omega}\theta} = I + \widehat{\omega}\sin\theta + \widehat{\omega}^2(1 - \cos\theta)$ （Rodrigues 公式，用于计算旋转矩阵） |
| `(2.23)` | $\bar{g}_{ab} = \begin{bmatrix} R_{ab} & p_{ab} \\ 0 & 1 \end{bmatrix}$ （齐次表示） |
| `(2.24)` | $\bar{g}_{ac} = \bar{g}_{ab}\,\bar{g}_{bc}$ （齐次变换的复合） |
| `(2.30)` | $\widehat{\xi}^{\vee} = \begin{bmatrix} v \\ \omega \end{bmatrix}$ （vee 算子，从 $se(3)$ 提取 twist 坐标） |
| `(2.36)` | $\displaystyle e^{\widehat{\xi}\theta} = \begin{bmatrix} e^{\widehat{\omega}\theta} & (I - e^{\widehat{\omega}\theta})\widehat{\omega}v + \omega\omega^T v\,\theta \\ 0 & 1 \end{bmatrix}$ （计算 $e^{\widehat{\xi}\theta}$） |
| `(2.40)` | $g = \begin{bmatrix} e^{\widehat{\omega}\theta} & (I - e^{\widehat{\omega}\theta})q + h\omega\theta \\ 0 & 1 \end{bmatrix}$ （由 $\xi = (v,\omega)$ 生成的螺旋运动） |
| `(2.45)` | $g_{ab}(\theta) = e^{\widehat{\xi}\theta}\,g_{ab}(0)$ （正运动学 POE 单关节） |
| `(2.58)` | $\operatorname{Ad}_g = \begin{bmatrix} R & \widehat{p}R \\ 0 & R \end{bmatrix}$ （Adjoint 变换矩阵） |

---

> **统计**: 共 13 个课件 PDF，**115 处**公式引用，去重后约 **60 个**独立公式编号
